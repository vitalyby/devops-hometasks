apiVersion: v1
kind: Service
metadata:
  name: wcg-service
spec:
  replicas: 3
  selector:
    app: word-cloud-generator
  ports:
    - name: http
      port: 8888
      protocol: TCP
      targetPort: 8888
